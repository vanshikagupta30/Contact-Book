<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://kit.fontawesome.com/37b9dc8ddb.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/home.css">
</head>
<body>
    
    <h1 id="page-heading">
        <i class="fas fa-address-book"></i>
        My Contact List
    </h1>
    <div id="contact-list-container">
        <ul>
            <% for(i of contactLists) { %>
                <li class="contact">
                    <div class="details">
                        <div id="name">
                            <%= i.name %>
                         </div>
                         <div id="phone">
                             <%= i.phone_number %>
                         </div>
                    
                        <div class="delete">
                            <!-- <a href="/delete-contact/<%= i.phone_number %>"> -->
                            <!-- <a href="/delete-contact/?phone=<%= i.phone_number %>&name=<%= i.name %>"> -->
                            <!-- <a href="/delete-contact/?phone=<%= i.phone_number %>"> -->
                            <a href="/delete-contact/?id=<%= i._id %>"> 
                                <i class="fas fa-window-close"></i>
                            </a>
                        </div>
                    </div>
                </li>
            <% } %>
        </ul>
    </div>

    <form id="contact-form" action="/create-contact" method="POST">
        <input type="text" name="name" placeholder="Enter Name" required>
        <input type="number" name="phone_number" placeholder="Enter Phone Number" required>

        <button type="submit">Add Contact</button>
    </form>

    <script type="text/javascript" src="/js/home.js"></script>

</body>
</html>